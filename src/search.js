import React from "react";

const Search = () => (
    <div className="search grid">
        <input className="input grid-item" placeholder="type something"></input>
    </div>
);

export default Search;
